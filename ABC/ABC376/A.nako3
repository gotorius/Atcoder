!「なでしこ3」
N = 入力[0].数値取得()      # n の取得
C = 入力[1].数値取得()      # c の取得
T = []                      # 時刻リスト

N回、{
    T[それ] = 入力[それ + 2].数値取得()  # t の各要素を取得
}

カウント = 1
A = 0

(0からN-2まで)の範囲で、{ |I|
    もし、T[I+1] - T[A] >= Cならば、
        カウント++
        A = I+1
    ここまで
}

カウントを表示
