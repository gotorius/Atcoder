#include <bits/stdc++.h>
using namespace std;
#define rep(i,n) for(int i=0; i<(n); i++)
using ll = long long;
using P = pair<int,int>;

int main(void){
    int n;
    cin >> n;
    if(n >= 200 && n < 300){
        cout << "Success" << endl;
    }else{
        cout << "Failure" << endl;
    }
    return 0;
}