#include <bits/stdc++.h>
using namespace std;
#define rep(i,n) for(int i=0; i<(n); i++)
using ll = long long;
using P = pair<int,int>;

int main() {
    ll q;
    cin >> q;

    vector<int> qn(q);
    vector<char> qc(q);
    rep(i,q) cin >> qn[i] >> qc[i];
    



}
